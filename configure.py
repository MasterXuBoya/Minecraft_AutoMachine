config = {
    #从PC端截屏时，截取区域左上角相对桌面的x坐标
    'projection_x': 68,
    #从PC端截屏时，截取区域左上角相对桌面的y坐标
    'projection_y': 110,
    #从PC端截屏时，截取区域的宽度
    'projection_width': 324,
    #从PC端截屏时，截取区域的高度
    'projection_height': 324,

    'grid_w':36,
    'grid_num':9,
}